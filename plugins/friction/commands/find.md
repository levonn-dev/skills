---
description: Scan this session for unlogged frictions and append them to FRICTION.md
---

Run a friction review of this session:

1. Read `FRICTION.md` at the project root if it exists, so you know what is already logged.
2. Re-read this session from the beginning. Collect every friction that matches the friction skill's "What counts" list and is not already logged.
3. Append one bullet per new friction to `FRICTION.md`, using the skill's entry format. Create the file with a `# Friction log` heading if it does not exist.
4. Reply with the entries you added, or state that none were found.
